<ul class="nav pull-right">
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown">Welcome, User <b
                        class="caret"></b></a>
            <ul class="dropdown-menu text-center">
                <li><a href="#"><i class="icon-cog"></i> Profile</a></li>
                <li class="divider"></li>
                <li><a href="{{ path('app_logout') }}"><i class="icon-off"></i> Logout</a></li>
            </ul>
        </li>
    {% else %}
        <li class="d-flex flex-row">
            <button type="button" class="btn-sm btn-outline-primary mr-1">Login</button>
            <button type="button" class="btn-sm btn-outline-success">Register</button>
        </li>
    {% endif %}
</ul>